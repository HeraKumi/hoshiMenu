language: cpp
compiler: gcc
os: linux
addon:
        sources:
            - ubuntu-toolchain-r test
        packages:
            - cmake-3.10
            - make
            - ninja-1.5
            - libncurses-dev
script:
        - mkdir build && cd build
        - cmake .. && make hoshiMenu
